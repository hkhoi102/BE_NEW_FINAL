# Google Gemini (recommended - free tier available)
GOOGLE_API_KEY=your-google-api-key-here
MODEL_NAME=gemini-2.5-flash
USE_GEMINI=true
# Available models: gemini-2.5-flash (fast), gemini-2.5-pro (powerful), gemini-pro-latest
# Check available models: GET http://localhost:8000/gemini/models

# OpenAI (fallback, optional)
# OPENAI_API_KEY=sk-xxxxxxxx
# MODEL_NAME=gpt-3.5-turbo

# Cross-database connection: connect to one schema you have SELECT on (e.g. product_db)
# For Docker: use 172.17.0.1 (Docker host IP)
# For local development: use localhost
MYSQL_URL=mysql+pymysql://reader:reader@172.17.0.1:3306/product_db

# RAG data paths
DOCS_DIR=./data/docs
CHROMA_DIR=./data/chroma

